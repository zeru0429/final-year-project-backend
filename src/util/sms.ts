// import { ACCOUNT_ID, AUTH_TOKEN, PHONE } from "../config/secrets.js";
// import twilio from 'twilio';

// const client = twilio(ACCOUNT_ID, AUTH_TOKEN);

// export const sendSMS = (sendTo: string, message: string) => {
//   client.messages.create({
//     body: message,
//     to: sendTo,
//     from: PHONE,
//   })
//   .then((message: any) => console.log(message.sid))
//   .catch((error: any) => console.error(error)); // Add error handling
// };
